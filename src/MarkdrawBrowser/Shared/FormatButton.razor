<div class="field@(ScrollFix ? " scroll-fix" : "")">
  <button class="button" disabled="@Disabled" @onclick="onclick">
    <span class="icon is-small">
      <i class="iconify" data-icon="mdi-@(Icon ?? "help")"></i>
    </span>
  </button>
</div>